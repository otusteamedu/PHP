upstream backend  {
  # -- Round Robin by default : )
  server backend_nginx_1;
  server backend_nginx_2;
  server backend_nginx_3;
}

server {
  listen 80;
  server_name localhost;
  location / {
    proxy_pass  http://backend;
  }
}