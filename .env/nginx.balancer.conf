upstream backend  {
  # -- Round Robin by default : )
  # random
  # least_conn
  server backend1;
  server backend2;
  server backend3;
}

server {
  listen 80;
  server_name localhost;
  location / {
    proxy_pass  http://backend;
  }
}