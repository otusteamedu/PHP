version: '3'

services:
    app_server:
        build: ./fpm
        working_dir: /var/www/html
        volumes:
            - ./../code:/var/www/html
            - ./../socket:/var/run
    app_client:
        build: ./fpm
        working_dir: /var/www/html
        volumes:
            - ./../code:/var/www/html
            - ./../socket:/var/run
