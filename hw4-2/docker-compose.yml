# версия синтаксиса
version: '3'

# в этом блоке мы описываем контейнеры, которые будут запускаться
services:
  #Контейнер с PHP, назовём его app
  app:
    # Если нет секции build, то система будет искать образ в репозиториях
    build:
      context: ./fpm
      dockerfile: Dockerfile
    image: myapp/php # имя будущего образа
    container_name: app # имя контейнера после запуска
    volumes:
      - ./code:/var/www/html
    # мы можем создать для контейнеров внутреннюю сеть
    networks:
      - app-network

  # Socket server
  socket-server-app:
    build:
      context: ./fpm
      dockerfile: Dockerfile
    image: myapp/php # имя будущего образа
    container_name: socket-server-app # имя контейнера после запуска
    volumes:
      - ./code:/var/www/html
    networks:
      - app-network

  # Nginx
#  webserver:
#    build:
#      context: ./nginx
#      dockerfile: Dockerfile
#    image: myapp/nginx
#    container_name: webserver
#    # проброс портов
#    ports:
#      - "8080:80"
#    #      - "443:443"
#    volumes:
#      - ./code:/var/www/html
#    networks:
#      - app-network

#Docker Networks
networks:
  app-network:
    driver: bridge