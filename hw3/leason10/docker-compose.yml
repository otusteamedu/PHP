version: '3.5'

services:
  db:
    image: postgres:10
    environment:
      - POSTGRES_PASSWORD=toor
    ports:
    - 5432:5432
    volumes:
    - ./:/tmp
    - ./data:/docker-entrypoint-initdb.d

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080