server {
    listen 8090;
    server_tokens off;

    location / {
        proxy_set_header    Host $host;
        proxy_set_header    X_Real_IP $remote_addr;
        proxy_pass          http://api;
        proxy_ssl_session_reuse off;
        proxy_redirect      off;

    }
}
