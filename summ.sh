#!/bin/bash

re='^[-]?[0-9]+\.?[0-9]*$'

if [[ ! $# -eq 2 ]]; then
    echo "Неверное количество аргументов"
    exit 1
elif [[ ! $1 =~ $re ]] || [[ ! $2 =~ $re ]]; then
    echo "Неверные  аргументы,передайте число"
    exit 1
else
    echo $1 + $2 |bc
fi