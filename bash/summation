#!/bin/bash

if (echo "$1" | grep -E -q "^-?[0-9]+$" && echo "$2" | grep -E -q "^-?[0-9]+$"); then
    echo $(($1 + $2))
else
    echo "Ошибка! Для корректной работы передайте скрипту 2 целых числа"
fi
