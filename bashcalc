!/bin/bash
echo "Введите первое число"
read int1
echo "Введите второе число"
read int2
sum=0
reg='^[-+]?\d*\.?\d*$'
for var in $int1 $int2
do
result=$(echo $var | grep -P "$reg")
if [[ $result = 0 ]] ; then
	echo "Ошибка, введны не числа" >&2; exit 1
fi
done
echo $int1+$int2 | bc
