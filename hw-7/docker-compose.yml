version: '2.2'

services:
  db:
    build:
      context: ./docker
      dockerfile: Dockerfile
    restart: always
    ports:
      - 5433:5432
    environment:
      POSTGRES_PASSWORD: otus

  adminer:
    image: adminer
    restart: always
    ports:
      - 8880:8080

