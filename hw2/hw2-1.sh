read -p "Введите первое слагаемое: " a
read -p "Введите второе слагаемое: " b
reg_num='^[+-]?[0-9]+([.][0-9]+)?$'
while ! [[ $a =~ $reg_num && $b =~ $reg_num ]] ;
do
    echo "Вы ввели слагаемые с ошибкой. Введите числовые параметры";
    read -p "Введите первое слагаемое: " a
	read -p "Введите второе слагаемое: " b
done
sum=$(echo $a + $b | bc)
echo "Cумма: " $sum