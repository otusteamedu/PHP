#!/bin/bash
echo "Please input first number!"
read x
echo "Please input second number!"
read y

if [[ ${x} =~ (^\-?[0-9]+(\.[0-9]+)?$) && ${y} =~ (^\-?[0-9]+(\.[0-9]+)?$) ]];
then
    echo | awk -v n1=$x -v n2=$y '{ print n1 + n2 }'
else
    echo "Wrong parameters, try again"
fi

