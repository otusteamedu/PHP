server {

    listen  80;
    server_name hw1.otus-lessons.com;

    charset utf-8;

    root /var/www/hm1/;

    location / {
        root /var/www/hm1/;

        index index.html;

        try_files $uri $uri/ @fallback;
    }
}
