version: '3'
services:
  server:
    image: php
    container_name: server
    volumes:
    - ./tmp
    links:
      - client
  client:
    image: php
    container_name: client
    volumes:
    - ./tmp
volumes:
  tmp: {}
