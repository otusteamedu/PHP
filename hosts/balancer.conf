worker_processes 4;

events { 
    worker_connections 1024; 
}

http {

    upstream backend {
        server nginx1;
        server nginx2;
        server nginx3;
    }

    server {
        listen 80;

        location / {
            proxy_pass         http://backend;
            proxy_set_header   Host $host;
        }
    }
}