version: '2.2'

services:
  server:
    build:
      context: ./docker/server
      dockerfile: Dockerfile
    volumes:
      - ./:/var/www/html
      - ./otus.sock:/var/www/html/otus.sock
  client:
    build:
      context: ./docker/client
      dockerfile: Dockerfile
    volumes:
      - ./:/var/www/html
      - ./otus.sock:/var/www/html/otus.sock
