version: '3'

services:

  db:
    build:
      context: ./docker/postgres 
    restart: always
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080