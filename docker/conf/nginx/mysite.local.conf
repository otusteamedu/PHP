server {
    listen 80;
    server_name mysite.local;
    root /var/www/mysite.local;
    index index.html;

    location ~* .(jpg|jpeg|gif|css|png|js|ico|html)$ {
        access_log off;
        expires max;
    }
}
