FROM nginx:1.16.1-alpine

RUN adduser -D -S -h /var/cache/nginx -s /sbin/nologin -G www-data www-data

COPY ./balancer/nginx.conf /etc/nginx/nginx.conf
COPY ./balancer/default.conf /etc/nginx/conf.d/default.conf
COPY ./balancer/upstream.conf /etc/nginx/upstream.conf

WORKDIR /var/www
