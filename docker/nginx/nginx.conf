user  app;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

worker_processes  auto;
events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  65;

    #gzip  on;

    include /etc/nginx/conf.d/*.conf;
}


#events {
#    use epoll;
#    worker_connections 1024;
#    multi_accept on;
#}

#http {
#    include       /etc/nginx/mime.types;
#    default_type  application/octet-stream;
#
#    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
#                      '$status $body_bytes_sent "$http_referer" '
#                      '"$http_user_agent" "$http_x_forwarded_for"';
#
#    access_log  /var/log/nginx/access.log  main;
#
#    keepalive_timeout  30;
#    keepalive_requests 100;
#
#    client_max_body_size  1m;
#    client_body_timeout 10;
#    reset_timedout_connection on;
#    send_timeout 2;
#    sendfile on;
#    tcp_nodelay on;
#    tcp_nopush on;
#
#    gzip on;
#    gzip_disable "msie6";
#    gzip_types text/plain text/css application/json application/x-javascript text/xml application/xml application/xml+rss text/javascript application/javascript;
#
#    open_file_cache max=200000 inactive=20s;
#    open_file_cache_valid 30s;
#    open_file_cache_min_uses 2;
#    open_file_cache_errors on;
#
#    include /etc/nginx/conf.d/*.conf;
#}


#docker run -d --rm --name hw5 -it mar4ehk0/hw5
#docker exec -it hw5 sh

#docker build --build-arg UID=3000 -t c_php .
#docker run -d --rm --name php -it mar4ehk0/php
#docker exec -it php sh

#docker build --build-arg UID=3000 -t c_nginx .
#docker run -d --rm --name nginx -it c_nginx
#docker exec -it nginx sh

#mar1.home:8080

