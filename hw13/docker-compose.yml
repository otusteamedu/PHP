version: "3"

volumes:
  redis_data:

services:
  php:
    build:
      context: ./docker/php 
    tty: true
    environment:
      - 'APP_PATH=${APP_PATH}'
    volumes:
      - './app:${APP_PATH}'

  redis:
    image: 'redis:alpine'
    restart: always
    volumes:
      - redis_data:/data