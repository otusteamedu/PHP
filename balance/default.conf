upstream app {
    least_conn;

    server app1;
    server app2;
    server app3;
    server app4;
    server app5;
}

server {
    listen       80;
    location / {
        proxy_pass http://app;
    }
}