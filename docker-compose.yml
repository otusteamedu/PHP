version: '3'
services:
<<<<<<< HEAD
  sender:
    build:
      context: ./docker/sender
      dockerfile: Dockerfile
    volumes:
      - ./sender:/app
      - ./.env:/app/.env
      - ./docker/tmp:/var/run/
    depends_on:
      - rabbitmq
  receiver:
    image: php:7.4-cli
    working_dir: /app
    volumes:
      - ./receiver:/app
      - ./.env:/app/.env
    depends_on:
      - rabbitmq
    command: ["./wait-for-rabbitmq.sh", "${RABBITMQ_DOCKER_HOST}",  "php", "./receiver.php"]
  nginx:
    image: nginx:1.19.0-alpine
    ports:
      - 80:80
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./sender:/app
      - ./docker/tmp:/var/run/
    depends_on:
      - sender
=======
  php-fpm:
    build:
      context: docker/php-fpm
    volumes:
      - ./:/var/www
    environment:
      XDEBUG_CONFIG: "remote_host=host.docker.internal remote_enable=1"
      PHP_IDE_CONFIG: "serverName=Docker"
    networks:
      - internal

  nginx:
    build:
      context: docker/nginx
    volumes:
      - ./:/var/www
    ports:
      - "80:80"
    depends_on:
      - php-fpm
    networks:
      - internal

>>>>>>> KBelov/hw27
  rabbitmq:
    image: rabbitmq:3.6.10-management
    ports:
      - 5672:5672
      - 15672:15672
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER}
<<<<<<< HEAD
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASS}
=======
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASS}
    networks:
      - internal

  mongodb:
    image: mongo
    restart: always
    ports:
      - '27017:27017'
    volumes:
      - './docker/mongodb:/data/db'
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
    networks:
      - internal

networks:
  internal:
>>>>>>> KBelov/hw27
