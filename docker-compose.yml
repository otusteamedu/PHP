version: '3'
services:
    nginx:
        build:
            context: ./docker
            dockerfile: nginx.docker
        ports:
            - "8081:80"
        volumes:
            - ./public:/var/www/public
