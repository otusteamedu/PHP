version: '3.1'
services:
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    ports:
      - "1234:3306"
    environment:
      MYSQL_ROOT_PASSWORD: otus
    volumes:
      - ~/mysql:/var/lib/mysql