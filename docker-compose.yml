version: '3.8'
services:
    web:
        image: nginx
        volumes:
            - ./config/nginx/default.conf:/etc/nginx/conf.d/default.conf
            - ./www:/var/www/hello/
        ports:
            - 8081:80
        restart: always