version: "3"

services:
    nginx:
        image: nginx
        ports: 
            - 80:80
        volumes:
            - ./www:/www
 
    php:
        build: .
        volumes:
            - ./www:/www
