version: '3.1'

services:

  db:
    image: postgres
    restart: always
    ports:
    - 80:5432
    environment:
      POSTGRES_PASSWORD: example
    volumes:
    - ./database:/var/lib/postgresql/data

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
