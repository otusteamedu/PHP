version: '2'

services:
  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx/core:/etc/nginx/conf.d
      - ./hw5:/var/www/html
      - ./nginx/Logs:/var/log/nginx/
      - ./:/usr/share/nginx/html/
    links:
      - php

  php:
    build: ./php

    volumes:
      - ./hw5:/var/www/html