events {}

http {
    upstream backend  {
      server nginx-server-1:80 weight=10 max_fails=3 fail_timeout=40s;
      server nginx-server-2:80 weight=4 max_fails=2;
    }

    server {
        listen 80;
        location / {
            proxy_pass http://backend;
        }
    }
}