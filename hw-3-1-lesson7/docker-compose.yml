version: '3.1'

services:

  # Use postgres/example user/password credentials
  db:
    build:
      context: ./docker
#    restart: always
    ports:
      - ${DB_PORT}:5432
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}