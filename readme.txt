Превое ДЗ (hw1-1).
//////////////////////////
2. Виртуальные машины. Развернуть Homestead VM при помощи Vagrant и VirtualBox

Поставил Vagrant.
VB 6 версии.
В Vagrantте запустил vagrant init laravel/homestead всё норм.
После ввёл команду vagrant up, ругался на шифрование. Пришлось добавить vagrant  в доверенные у антивируса.
Виртуалка появилась в VB, но не запускалась.
Поставил VB 5 версии, тогда виртуалка запустилась.
Если Byobu Terminal это тепримал, то он не запускается, что расстроило.
Оказалось криво поставил виртуалку, сделал по инструкции с https://laravel.ru/docs/v5/homestead.
Результат на скрине 1.
//////////////////////////
1. Docker
1.1. Установить Docker себе на машину
1.2. С помощью Dockerfile настроить статический сайт (можно использовать nginx образ)

$ sudo apt-get update

$ sudo apt-get install \
    apt-transport-https \
    ca-certificates \
    curl \
    gnupg-agent \
    software-properties-common
$ curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
$ sudo add-apt-repository \
   "deb [arch=amd64] https://download.docker.com/linux/ubuntu \
   $(lsb_release -cs) \
   stable"
 $ sudo apt-get install docker-ce docker-ce-cli containerd.io
Скрин2.

Готовый образ: 
$ docker run prakhar1989/static-site
Запуск:
$ docker run -d -P --name static-site prakhar1989/static-site
Скрин3.
Заходим на порт.
Скрин4.
//////////////////////
3. Выберите в качестве примера свою текущую компанию (или компанию, в которой хотите работать), коротко опишите ее (количество сотрудников, сфера, приоритеты)
Сравните целесообразность разворачивания своей инфраструктуры или аренды публичного облака (можно выбрать любого провайдера)
Компания ТЭК-Торг, около 250 сотрудников. Электронная торговая площадка. Достаточно большой поток обращений на сайт и несколько отдельных секций - отдельных сайтов по сути.
Учитывая необходимость хранения конфеденциальных данных, а также высокую загрузку серверов в отдельные периоды времени и большой штат разработчиков считаю нецелесообразно арендовать облако.



