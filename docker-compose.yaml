version: '3'

services:
  nginx:
    image: nginx
    volumes:
      - './:/srv/app'
      - './docker/nginx/site.conf:/etc/nginx/conf.d/default.conf'
    ports:
    - '8080:80'
  php:
    build:
      context: ./docker/php
    volumes:
      - './:/srv/app'
