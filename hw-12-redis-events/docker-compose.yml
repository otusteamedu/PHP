version: '3.8'

services:

  redis:
    build:
      context: ./docker/redis

  php:
    build:
      context: ./docker/php-fpm
      args:
        UID: "3000"
    volumes:
      - ./app:/var/www/hw
      - ./docker/sock:/sock

  nginx:
    build:
      context: ./docker/nginx
      args:
        UID: "3000"
    ports:
      - 80:80
    depends_on:
      - php
    volumes:
      - ./app:/var/www/hw
      - ./docker/sock:/sock
    links:
      - php

  composer:
    build:
      context: ./docker/composer
    depends_on:
      - php
    volumes:
      - ./app:/var/www/hw