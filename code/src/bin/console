#!/usr/bin/env php

<?php

use App\ConsoleApp;

require __DIR__ . '/../../vendor/autoload.php';

try {
    $app = new ConsoleApp();
    $app->run();
}
catch(Exception $e){
    echo $e->getMessage(), PHP_EOL;
    echo $e->getFile() . ': ' . $e->getLine(), PHP_EOL;
}
