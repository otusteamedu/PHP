version: '3.8'
services:
  php_server:
    container_name: php_server
    build:
      context: ./php-fpm
    volumes:
      - ./app:/var/www/socket.test
  php_client:
    container_name: php_client
    build:
      context: ./php-fpm
    volumes_from:
      - php_server