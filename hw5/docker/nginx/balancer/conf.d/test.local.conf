server {
    listen 80;
    server_name test.local;

    location / {
        proxy_pass http://backend;
    }
}

upstream backend {
    server backend1_nginx;
    server backend2_nginx;
    server backend3_nginx;
}
