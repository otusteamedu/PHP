version: '3'
services:
  web:
    image: nginx:latest
    ports:
    - "80:80"
    volumes:
    - ./docker/nginx:/etc/nginx/conf.d
    - ./www:/var/www
    depends_on:
    - phpfpm
  phpfpm:
    build: ./docker/phpfpm
    volumes:
    - ./www:/var/www