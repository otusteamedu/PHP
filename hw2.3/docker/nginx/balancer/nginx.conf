events {}

http {
    upstream backend  {
      server 172.30.0.2:80 weight=10 max_fails=3 fail_timeout=30s;
      server 172.30.0.4:80 weight=5 max_fails=2;
    }

    server {
        listen 80;
        location / {
            proxy_pass http://backend;
        }
    }
}
