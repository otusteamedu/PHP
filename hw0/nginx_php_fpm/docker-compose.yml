web:
  image: "nginx:latest"
  ports:
    - "80:80"
  volumes:
    - ./app:/var/www/app
    - ./app.local.conf:/etc/nginx/conf.d/app.local.conf
  links:
    - php
php:
  image: "php:7-fpm"
  volumes: 
    - ./app:/var/www/app
