events {}

http {
    upstream backend  {
      server 172.30.0.2:80 weight=10;
      server 172.30.0.4:80 weight=5;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }
    }
}
