server {
  listen   80;

  server_name hw1;

  root /var/www/hw1/public;
  
  index index.html index.htm;
  
  location / {
   default_type "text/html";
   try_files $uri.html $uri $uri/ /index.html;
  }
}