server {
  listen   80;

  server_name mysite.local;

  root /var/www/mysite.local/public;
  
  index index.html index.htm;
  
  location / {
   default_type "text/html";
   try_files $uri.html $uri $uri/ /index.html;
  }
}