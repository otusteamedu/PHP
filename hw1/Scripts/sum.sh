#! /bin/bash

if [ -n "$1" ]; then

  a=$1
  b=$2

 if ! [[ $a =~ ^-?[0-9]+[.]?[0-9]*$ ]] || ! [[ $b =~ ^-?[0-9]+[.]?[0-9]*$ ]]
 then
  echo "Параметры должены быть числом"
  exit 1;
  fi

  echo -n "$a + $b = "
  echo "$a+$b" | bc

else

  echo "Укажите два параметра"

fi
