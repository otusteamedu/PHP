server {

    listen 80;
    server_name site.local;

    root /var/www/localhost/htdocs;

    index index.html;

    location ~* .(jpg|jpeg|gif|css|png|js|ico|html)$ {
        access_log off;
        expires max;
    }

}
